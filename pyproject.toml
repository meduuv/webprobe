[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "webprobe"
version = "0.1.0"
description = "Safe URL parsing and validation helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/webprobe"]
