# webprobe

Safe, read-only URL parsing and HTTP/HTTPS validation helpers.

```python
from webprobe import parse_url, is_http_url
```

Run `pytest` to test. License: MIT.

Built by meduuv. `guns.lol/meduu`
